---
description: Restore context — preflight, graph, latest session logs, in-flight OpenSpec changes
---

Invoke the `supercharge` skill (Skill tool, `skill: supercharge`) and run its **start**
mode. Read the skill's `references/sessions.md` for the full protocol.

Restore context before touching code: run preflight, orient with the graph, read the
newest session logs for live state and resume commands, read the in-flight OpenSpec
changes, then report where we were, what is in flight, and the exact command that
resumes it.

$ARGUMENTS
